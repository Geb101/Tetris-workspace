# Лог действий

## Назначение

В этом файле фиксируются технические действия, связанные с подготовкой документации, BrowserSkill, Google Docs и Google Docs MCP.

## 22.09.2026 — анализ Google Docs

- Подключён BrowserSkill для работы с реальным браузером.
- BrowserSkill установлен глобально для Windows.
- Проверена версия CLI: `bsk 0.3.0`.
- Выполнена диагностика BrowserSkill через `bsk doctor`.
- Подтверждено подключение браузера и расширения BrowserSkill.
- Google Docs открыт через BrowserSkill.
- Первоначально документ был доступен через аккаунт `georgestalinskiy@gmail.com`.
- Для работы с нужными файлами выбран аккаунт `georgegoncharov101@gmail.com`.
- Проверена структура документа `Tetris tech-docs`.
- Обнаружены вкладки:
  - `Introduction to the Game`;
  - `Запуск игры и первые действия`;
  - `Проверка себя`;
  - `Ссылки для проверки и версии игры`.
- Проведён визуальный анализ практической главы.
- Зафиксированы проблемы с переносом текста и изображений между страницами.
- Отмечено, что изображения для отдельных действий показывают слишком много интерфейса.
- Подготовлены рекомендации по crop, выравниванию и последовательности кадров.

## 22.09.2026 — проверка файлов в Google Drive

- В аккаунте `georgegoncharov101@gmail.com` открыт каталог `tetris-doc/source-pic`.
- Найдены папки и материалы:
  - `pressUP/`;
  - `pressLeftOrRight/`;
  - `pressDown/`;
  - `endgame/`;
  - `gameplay/`;
  - `gameplay_example.png`;
  - `tetramino.png`.
- В папке `pressUP/` обнаружены кадры `press_Up1-0.png` — `press_Up4-0.png`.
- В папке `endgame/` обнаружены кадры `end_game1-0.png` — `end_game3-0.png`.
- Определён рекомендуемый порядок вставки материалов в практическую главу.

## 22.09.2026 — начало верстки

- Выбран эталонный блок `Rotating a Piece`.
- Определён шаблон блока:

```text
Action title
Short instruction
Image sequence
Expected result
```

- Определено, что кадры должны быть оформлены как последовательность `Before → Action → After`.
- Выполнена пробная операция позиционирования в Google Docs.
- Пробный разрыв страницы оказался установлен не в целевом месте и был отменён.
- Изображения вслепую не вставлялись, чтобы не повредить структуру документа.

## Текущий статус

- BrowserSkill установлен и работает.
- Доступ к Google Drive через BrowserSkill подтверждён аккаунтом `georgegoncharov101@gmail.com`.
- Структура Google Docs через BrowserSkill проверена.
- Google Docs MCP настроен глобально.
- OAuth-переменные присутствуют в пользовательском окружении.
- Чтение документа через Google Docs MCP пока не подтверждено: сервер возвращает `Unauthorized`.
- Для завершения настройки требуется повторная OAuth-авторизация и проверка разрешений Google Cloud OAuth-клиента.

## Ограничения безопасности

- OAuth Client Secret не хранится в Git.
- OAuth Client Secret не записывается в Markdown-файлы.
- Опубликованный ранее секрет рекомендуется отозвать и заменить новым.
- После изменения конфигурации или переменных среды OpenCode необходимо полностью перезапустить.

## 22.09.2026 — фиксация процесса и подготовка репозитория

- Создан документ `04-решения/Общий процесс.md` с полной схемой подготовки документации.
- В стек добавлены Antigravity (верстка) и Google Docs MCP (программный доступ к документу).
- Проведено обсуждение ошибок чтения документации с другом.
- Подтверждены принципы: пошаговое изложение, порядок «абстракция → конкретное управление», показ результата действия на кадрах.
- Запланирован тест документации на неопытном читателе.
- Обновлён `README.md` под публикацию репозитория.
- Добавлен `.gitignore`: исключены буферы, скриншоты, настройки Obsidian, временные файлы.
- Инициализирован локальный Git-репозиторий в `Tetris-workspace`.
- Решено добавить ссылку на репозиторий в итоговый Google Docs.
